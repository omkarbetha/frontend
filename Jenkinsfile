@Library('todo') _

todo(
     COMPONENT           : 'frontend',
      PROJECT_NAME       : "Todo",
      SLAVE_LABEL        : "NODEJS",
      SKIP_NEXUS_UPLOAD  : false,
      APP_TYPE           : "NGINX"
)
